<nav class="bg-dark">
    <div class="grid col-3 white p-1 align-center w-max-container mx-auto">
        <div></div>
        <div class="text-center text-3xl">
            <span>Twitter Clone</span>
        </div>
        <div class="self-end flex gap-2">
            <% if (locals.session?.username) { %>
                <span class="white">Hello, <%= session.username %></span>
                <a class="white no-underline hover:underline" href="<%= path('/logout') %>">Logout</a>
            <% } else { %>
                <a class="white no-underline hover:underline" href="<%= path('/login') %>">Log In</a>
                <a class="white no-underline hover:underline" href="<%= path('/register') %>">Register</a>
            <% } %>
        </div>
    </div>
</nav>